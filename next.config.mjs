/** @type {import('next').} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "res.cloudinary.com",
        port: "",
      },
    ],
  },
};
// module.exports = nextConfig;
export default nextConfig;
